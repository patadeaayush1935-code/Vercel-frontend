export default function Employers() {
  return (
    <section className="py-20 max-w-6xl mx-auto px-6 text-center" style={{ background: 'linear-gradient(180deg, #ffffff 0%, #eff6ff 100%)' }}>
      <h2 className="text-3xl font-semibold">Hire workers in minutes</h2>
      <p className="text-gray-600 mt-4">No resumes. No delays.</p>

      <button className="mt-6 bg-blue-600 text-white px-6 py-3 rounded-xl hover:bg-blue-700 transition">
        Post a Job
      </button>
    </section>
  );
}